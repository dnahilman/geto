name: Build Windows Desktop App

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build-windows:
    name: Build Windows Application
    runs-on: windows-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-pc-windows-msvc

      - name: Rust Cache
        uses: swatinem/rust-cache@v2

      - name: Install dependencies
        run: bun install

      - name: Build Windows Desktop App
        run: bun run build:desktop

      - name: List build outputs
        run: |
          Get-ChildItem -Path target -Recurse -Filter *.exe -ErrorAction SilentlyContinue | Select-Object FullName
          Get-ChildItem -Path target -Recurse -Filter *.msi -ErrorAction SilentlyContinue | Select-Object FullName
          Get-ChildItem -Path apps/desktop -Recurse -Filter *.exe -ErrorAction SilentlyContinue | Select-Object FullName
        shell: pwsh

      - name: Upload Windows Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: geto-windows-x64
          path: |
            target/release/bundle/nsis/*.exe
            target/release/bundle/msi/*.msi
            target/release/Geto.exe
            target/release/geto-desktop.exe
            apps/desktop/src-tauri/target/release/bundle/nsis/*.exe
            apps/desktop/src-tauri/target/release/bundle/msi/*.msi
            apps/desktop/src-tauri/target/release/Geto.exe
            apps/desktop/src-tauri/target/release/geto-desktop.exe
          if-no-files-found: warn

      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            target/release/bundle/nsis/*.exe
            target/release/bundle/msi/*.msi
            target/release/Geto.exe
            target/release/geto-desktop.exe
            apps/desktop/src-tauri/target/release/bundle/nsis/*.exe
            apps/desktop/src-tauri/target/release/bundle/msi/*.msi
            apps/desktop/src-tauri/target/release/Geto.exe
            apps/desktop/src-tauri/target/release/geto-desktop.exe
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
